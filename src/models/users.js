'use strict'

class User {
    constructor(_id, firstName, lastName, email, role) {
      this._id = _id;
      this.firstName = firstName;
      this.lastName = lastName;
      this.email = email;
      this.role = role;